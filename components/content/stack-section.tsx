import { cn } from '@/lib/utils'
import React from 'react'

export default function StackSection({
  className
}: {
  className?: string
}) {
  return (
    <section id="stack" className={cn('', className)}>
      
    </section>
  )
}
